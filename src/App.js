import { Components } from "./components/Components";
const { Header, Main, Footer } = Components;

export const App = () => {
    return (
        <>
            <Header />
            <Main />
            <Footer />
        </>
    );
};
